import React from 'react';
import "rbx/index.css";
import { Box, Container, Section } from 'rbx';

const NotFound = () => (
  <Section>
    <Container>
      <Box>Page not found!</Box>
    </Container>
  </Section>
);

export default NotFound;
